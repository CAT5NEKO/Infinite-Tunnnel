[build]
  publish = "dist"
  command = "pnpm install --frozen-lockfile && pnpm run build"

[dev]
  command = "pnpm run dev"
  port = 5173

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "24"
  PNPM_VERSION = "10.30.1"
